// Test break and continue statements in loops

// ===== Basic Break =====

// Simple break - exit loop early
x = 0;
while x < 10 {
    if x == 5 {
        break;
    }
    println x;
    x = x + 1;
}

// ===== Basic Continue =====

// Simple continue - skip even numbers
counter = 0;
while counter < 5 {
    counter = counter + 1;
    if counter == 2 || counter == 4 {
        continue;
    }
    println counter;
}

// ===== Break with Condition =====

// Find first number divisible by 7
num = 1;
while num < 100 {
    if num - num / 7 * 7 == 0 {
        println num;
        break;
    }
    num = num + 1;
}

// ===== Continue with Multiple Conditions =====

// Print numbers that are not 3 and not 6
i = 0;
while i < 10 {
    i = i + 1;
    if i == 3 {
        continue;
    }
    if i == 6 {
        continue;
    }
    println i;
}

// ===== Nested Loops with Break (outer loop) =====

// Break only affects innermost loop
outer = 0;
while outer < 3 {
    inner = 0;
    while inner < 5 {
        if inner == 3 {
            break;  // Breaks inner loop only
        }
        println inner;
        inner = inner + 1;
    }
    println 100;  // Should println 3 times
    outer = outer + 1;
}

// ===== Nested Loops with Continue =====

// Continue in nested loop
a = 0;
while a < 3 {
    b = 0;
    while b < 4 {
        b = b + 1;
        if b == 2 {
            continue;  // Skip when b == 2
        }
        println b;
    }
    println 200;
    a = a + 1;
}

// ===== Complex: Break and Continue Together =====

// Print odd numbers up to 10, but stop at 7
n = 0;
while n < 20 {
    n = n + 1;

    // Skip even numbers
    if n - n / 2 * 2 == 0 {
        continue;
    }

    // Stop at 7
    if n > 7 {
        break;
    }

    println n;
}

// ===== Break in Loop with Boolean Condition =====

// Search for target value
value = 0;
target = 42;
found = 0;

while value < 100 && !found {
    if value == target {
        println value;
        found = 1;
        break;
    }
    value = value + 1;
}

// ===== Continue with Negation =====

// Print values where condition is NOT met
x = 0;
while x < 6 {
    x = x + 1;
    if !(x == 2 || x == 5) {
        continue;  // Skip everything except 2 and 5
    }
    println x;
}

// ===== Infinite Loop with Break =====

// Loop forever until condition met
count = 0;
while 1 {
    count = count + 1;
    if count > 3 {
        break;
    }
    println count;
}

// ===== End Marker =====
println 9999;
